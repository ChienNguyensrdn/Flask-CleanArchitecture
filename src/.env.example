# Environment Configuration Example
# Copy this file to .env and fill in your values

# Flask Configuration
FLASK_ENV=development  # development, testing, production
SECRET_KEY=your-super-secret-key-change-this-in-production

# Database Configuration (REQUIRED)
# Format: mssql+pymssql://username:password@host:port/database
DATABASE_URI=mssql+pymssql://sa:YourPassword@127.0.0.1:1433/FlaskApiDB

# Development-specific database (optional, used if DATABASE_URI not set in development)
DEV_DATABASE_URI=mssql+pymssql://sa:YourPassword@127.0.0.1:1433/FlaskApiDB_Dev

# Testing-specific database (optional)
TEST_DATABASE_URI=mssql+pymssql://sa:YourPassword@127.0.0.1:1433/FlaskApiDB_Test

# Logging Configuration
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_DIR=logs

# Security Note:
# - Never commit your actual .env file to version control
# - Use strong, unique passwords in production
# - Generate a cryptographically secure SECRET_KEY:
#   python -c "import secrets; print(secrets.token_hex(32))"
